{
  "name": "irregular-words",
  "private": true,
  "scripts": {
    "setup": "npm ci --prefix api && npm ci --prefix web",
    "build": "nx run-many -t build -p api web",
    "build:start": "npm run build && npm run start:prod --prefix api",
    "start": "nx run api:serve",
    "api:dev": "nx run api:dev",
    "web:dev": "nx run web:dev"
  },
  "devDependencies": {
    "nx": "23.2.1"
  }
}
