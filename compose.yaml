services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API: ${VITE_API:-/api}
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
    restart: unless-stopped
